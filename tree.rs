use std::{env, path};
fn main() {
    // Read artument values.
    let args: Vec<String> = env::args().collect();
    // If no path is given, set it to the current directory.
    let mut target_dir = ".";
}
